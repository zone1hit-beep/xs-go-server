name: xs_go_server
description: Backend API cho app học tiếng Nhật XS GO (Dart + shelf + SQLite + Claude AI).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0
  crypto: ^3.0.3
  http: ^1.2.0
  dart_jsonwebtoken: ^2.14.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.31.2
